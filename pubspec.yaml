name: fss_interview
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.16.2 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  dio: ^4.0.6
  cached_network_image: ^3.2.0
  font_awesome_flutter: ^10.2.1
  cupertino_icons: ^1.0.2
  fluttertoast: ^8.0.9
  flutter_svg: ^1.1.5
  shared_preferences: ^2.0.15
  image_picker: ^0.8.5+3
  permission_handler: ^10.0.1
  photo_view: ^0.14.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^1.0.0

flutter:

  uses-material-design: true

  assets:
   - images/ecommerce_logo.png

